import React from "react";
import { Stage, Layer } from "react-konva";
export default function DrawingCanvas() {
  return (
    <Stage width={600} height={400}>
      <Layer>
        {/* Add your content to the layer here (for now, leave it empty) */}
      </Layer>
    </Stage>
  );
}
